apply plugin: 'war'

dependencies {
    providedCompile configurations.jerseybundle
    providedCompile configurations.asm
    providedCompile project(path: ':core')
    providedCompile project(path: ':utils:logger')
}

war {
    
}